#./parallel_build.csh
#mepo status
#mepo checkout -b feature/eyang/PBLH # added by EG

# after making changes

git add <filenames of all changed files>
git commit -m “message describing what was changed”
git push
 
Running ‘git status’ will provide a list of modified and added files. 
